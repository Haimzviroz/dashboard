export enum NavBarOption {
  ACTIVITY = "activity",
  RELEASES = "releases",
  POLICY = "policy",
  DOCS = "docs",
}

export enum SideBarOption {
  CONTROL = "control",
  MAP = "map",
  DEVICES = "devices",
  APPS = "apps",
}