import { R_MAP_GROUP } from "@/apis/routes";
import LoginPage from "@/components/main/page";
import { FC } from "react"

const MapGroupPage: FC = () => {
  return <LoginPage routePath={R_MAP_GROUP}/>
}

export default MapGroupPage
